{
  "name": "vocab-forge",
  "version": "1.0.0",
  "description": "Browser-based vocabulary reviewer built for massive CSV/TSV imports",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
